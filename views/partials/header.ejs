<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= typeof title !== "undefined" ? title + " · Enterprise ITSM" : "Enterprise ITSM" %></title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="app-shell">
    <nav class="sidebar">
      <div class="brand">Enterprise ITSM</div>
      <a href="/" class="nav-link">Dashboard</a>
      <a href="/incidents" class="nav-link">Incidents</a>
      <div class="nav-section-label">Coming in Phase 2</div>
      <span class="nav-link disabled">Service Requests</span>
      <span class="nav-link disabled">Problems</span>
      <span class="nav-link disabled">Changes</span>
      <span class="nav-link disabled">Assets / CMDB</span>
      <span class="nav-link disabled">Knowledge Base</span>
      <div class="sidebar-footer">
        <div class="user-chip"><%= currentUser.name %> <span class="role-badge"><%= currentUser.role %></span></div>
        <form method="post" action="/logout"><button class="link-button">Sign out</button></form>
      </div>
    </nav>
    <main class="content">
